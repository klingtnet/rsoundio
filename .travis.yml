sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly
env:
  - CC=/usr/bin/gcc-4.8
  - CXX=/usr/bin/g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.8
      - g++-4.8
matrix:
    allow_failures:
        - rust: nightly
